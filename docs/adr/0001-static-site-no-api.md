# Static site, no public API

The original ask was a portfolio API. The product that shipped is a public static Site: Markdown files built by Next.js, hosted on Vercel, no `/api` routes. An API would be a second product (runtime, auth, versioning). Content edits are file edits plus a deploy.
